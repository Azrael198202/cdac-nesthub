# v8.5 Agent Runtime Parameter and Generated Artifact Chain Fix

## Fixed
- Agent profile creation now preserves parameter schema generated at runtime while clearing task-run values.
- Agents without uploaded artifacts no longer receive an uploaded-file policy accidentally.
- Task-scoped parameters are applied to in-memory participant copies before missing-parameter checks, without persisting values to the agent profile.
- Internal capability/intent summaries are rejected as final answer material.

## Quality Gates
- Python compile check.
- Smoke tests for agent schema retention, runtime parameter collection, task parameter merge, no durable value leakage, and final-answer guard.
