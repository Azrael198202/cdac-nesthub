# v7.2 - Task graph parameter contract NameError fix

- Fixed `AgentStudioService.create_task_graph` referencing undefined `parameter_contract`.
- Task graphs now use an explicit blank task-run scoped parameter contract.
- Prevents stale participant parameter values from leaking into uploaded-artifact task runs.
- Verified `agent_studio_message -> create_task_graph` no longer fails with `NameError`.
