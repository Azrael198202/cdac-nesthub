# v2.9.40 Runtime-native participant convergence

- Runtime temporal-observation participants are routed to runtime-generated local code execution.
- Runtime-native observation contracts disable web/API fallback.
- Delegated objective extraction is used before intent-family enforcement to avoid the outer envelope polluting the participant route.
- External structured information keeps API-first ordering from configuration/policy.
- SecretStore and source modules are preserved; runtime artifacts and secret values are excluded from the package.
