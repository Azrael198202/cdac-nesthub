{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Kill Port 8000 Listeners",
      "type": "shell",
      "command": "pids=($(lsof -ti tcp:8000 -sTCP:LISTEN)); (( ${#pids[@]} )) && kill -9 $pids; echo port_8000_ready",
      "problemMatcher": []
    }
  ]
}
