# v8.1 - Command Help UI

- Added a command help button in `apps/web/agent_studio.html`.
- Help dialog fetches `/api/agent-studio/command-set` and renders the effective command registry grouped by category.
- Shows command id, enabled/disabled state, description, priority order within category, and matching patterns.
- Uses the command registry as source of truth; no new command routing hardcode was added.
