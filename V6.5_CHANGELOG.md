# V6.5 Task Runtime Parameter Reset

- Agent profiles store parameter schema only; runtime values are no longer persisted on the agent.
- Each task execution receives fresh participant copies with empty runtime parameters.
- Resume after UI parameter collection applies values only to the active run copy.
- Delegated final delivery rejects internal prompt/contract text and only accepts explicit answer material.
- Generation verification remains structure/material based; no small-model factual judging is required.
