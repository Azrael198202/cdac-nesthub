{
  "version": "v2.7.3-source-clean",
  "description": "Runtime tool routing profile container. Concrete profiles are generated at runtime and stored under runtime/generated or supplied by deployment configuration.",
  "profiles": [],
  "text_cleanup": {
    "drop_phrases": [
      "create a task",
      "create task",
      "named",
      "which calls",
      "calls",
      "participant",
      "execute",
      "task"
    ]
  },
  "profile_schema": {
    "profile_id": "string",
    "operation": "string",
    "match_terms": [
      "string"
    ],
    "parameters": [
      {
        "name": "string",
        "patterns": [
          "regex string"
        ],
        "default": "string"
      }
    ],
    "requests": [
      {
        "url_template": "string",
        "response_type": "string",
        "timeout_seconds": "number"
      }
    ],
    "summary_heading": "string",
    "summary_fields": [
      {
        "label": "string",
        "path": "string",
        "suffix": "string"
      }
    ]
  }
}