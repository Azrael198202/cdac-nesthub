{
  "version": "v2.6.1-runtime-timezone-fallback",
  "description": "Runtime-configured timezone fallback offsets used when the host Python installation has no IANA timezone database. Code reads this file instead of embedding locale-specific fallback values.",
  "fallback_offsets_minutes": {
    "Asia/Tokyo": 540,
    "UTC": 0
  },
  "aliases": {
    "local": "Asia/Tokyo"
  }
}
