# v6.7 uploaded artifact UI bridge fix

- Added missing ToolCallExecutor._interaction_requests_from_validation.
- Missing runtime parameters now return human_interactions/UI requests instead of raising AttributeError.
- Improved uploaded artifact registry resolution for filename-based references.
- Normalized Windows-style registry paths before safe path resolution.
- Captured stdout from uploaded Python artifacts and promoted it to answer_material when the function prints instead of returning.
- Kept ai_core domain-neutral: no weather-specific or business-specific logic was added.
